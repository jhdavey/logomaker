"use strict";
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spread = (this && this.__spread) || function () {
    for (var ar = [], i = 0; i < arguments.length; i++) ar = ar.concat(__read(arguments[i]));
    return ar;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.validateHexRGB = void 0;
var validateHexRGB = function (rgbString) {
    if (!rgbString.startsWith('#'))
        return false;
    // Convert the hex color string to uppercase and remove the hash symbol
    rgbString = rgbString.toUpperCase().slice(1);
    if (rgbString.length !== 6 && rgbString.length !== 3)
        return false;
    var areAllValuesValidHex = __spread(rgbString).every(function (s) { return s.search(/[0-9A-F]/g) !== -1; });
    if (!areAllValuesValidHex)
        return false;
    return true;
};
exports.validateHexRGB = validateHexRGB;
