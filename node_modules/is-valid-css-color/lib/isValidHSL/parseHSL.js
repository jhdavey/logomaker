"use strict";
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.parseHSL = void 0;
var parseHSL = function (hslString) {
    var csvString = hslString.replace(/((hsla)|(hsl))|\(|\)|\s/g, '');
    var _a = __read(csvString.split(','), 4), hue = _a[0], saturation = _a[1], lightness = _a[2], alpha = _a[3];
    return { hue: hue, saturation: saturation, lightness: lightness, alpha: alpha !== null && alpha !== void 0 ? alpha : '' };
};
exports.parseHSL = parseHSL;
